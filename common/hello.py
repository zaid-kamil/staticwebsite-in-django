from fun1 import total_amount
from fun2 import total_area
amount = total_amount(10000, 15)
print(f'Amount = {amount}')

h = 10
w = 20
r = 5
ans = total_area(height=h, width=w, radius=r)
print(f'Area = {ans}')
